#include<stdio.h>
int main(void)
{
    int num;
    printf("Enter the number: \n");
    scanf("%d", &num);
    if(num / 3 == 0)
    printf("Number is multiple of 3 \n");
    else
   printf("Number is not multiple of 3 \n");
}
